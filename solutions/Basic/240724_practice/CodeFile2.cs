class Program
{
    static void Main(string[] args)
    {
        // init, start
        bool state = true;
        // 배경, 플레이어 배치, 초기화

        // loop, update, process
        while (state)
        {
            // 키 입력
            // 이동, 동적 이벤트
            // 렌더링
            // 프레임워크 ( 필요한 부분만 끼워넣기 )



            //state = false;
        }
    }
}